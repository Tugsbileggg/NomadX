# Амьд байршил — POC

Артистын **утаснаас** GPS уншиж, үйлчлүүлэгчийн **компьютер** дээрх газрын
зураг дээр амьдаар харуулна.

Энэ үе шатанд зөвхөн ажиллагаа — дизайныг дараа нь тааруулна.

## Урсгал

```
Expo (утас)                Supabase Realtime              Next.js (компьютер)
expo-location  ──fix──►   broadcast `loc:<ӨРӨӨ>`  ──fix──►  Leaflet газрын зураг
watchPositionAsync                                          marker + мөр + нарийвчлал
```

Тусдаа сервер шаардлагагүй — Supabase Realtime нь релей болно. DB хүснэгт ч
хэрэггүй (broadcast нь өгөгдөл хадгалдаггүй).

| Файл | Үүрэг |
| --- | --- |
| `app/src/app/share.tsx` | Expo дэлгэц — зөвшөөрөл, GPS, илгээлт |
| `app/src/lib/live-location.ts` | Протокол + throttle (утасны тал) |
| `frontend/src/app/share/page.tsx` | Вэб илгээгч `/share` — хөтчөөс, апп-гүйгээр |
| `frontend/src/app/track/page.tsx` | Вэб хуудас `/track` |
| `frontend/src/app/track/LiveMap.tsx` | Leaflet зураг, marker, мөр, "тасарсан" төлөв |
| `frontend/src/lib/live-location.ts` | Протокол (вэб тал) |
| `server/scripts/fake-walk.mjs` | Утасгүйгээр турших хуурамч алхагч |
| `server/scripts/rt-check.mjs` | Realtime холболтыг шалгах |

## Турших

### A. Утасгүйгээр (хамгийн хурдан)

```bash
npm --prefix frontend run dev
```

Хөтчөөр `http://localhost:3000/track?room=UB-1024` нээгээд, өөр терминалд:

```bash
cd server && npm run rt:walk UB-1024 30
```

Marker хөдөлж, ард нь мөр үлдэнэ.

### B. Жинхэнэ утсаар — хөтчөөр (апп суулгахгүй)

Хамгийн найдвартай зам. Утас зөвхөн хөтөч ашиглана.

```bash
npm --prefix frontend run dev -- --experimental-https -H 0.0.0.0
```

- Утсан дээр: `https://<таны-IP>:3000/share?room=UB-1024`
- Компьютер дээр: `https://<таны-IP>:3000/track?room=UB-1024`

> **HTTPS заавал.** `http://` дээр хөтөч GPS зөвшөөрөл огт асуухгүй.
> `localhost` бол цорын ганц үл хамаарах тохиолдол. `--experimental-https`
> нь өөрөө гарын үсэг зурсан гэрчилгээ үүсгэдэг тул утас "Not Private"
> гэж анхааруулна — Advanced → Proceed гэж үргэлжлүүлнэ.

### C. Expo апп-аар

```bash
npm --prefix app run start
```

Утсандаа **Expo Go**-гоор QR уншуулж нээнэ → **Байршил** таб → өрөөний код →
**Байршил хуваалцаж эхлэх**.

> Төсөл **Expo SDK 54** дээр (RN 0.81.5, React 19.1.0) — хуучин iOS дээрх
> Expo Go-той нийцүүлэхийн тулд 57-оос буулгасан. SDK солих бол
> `app/package.json` доторх бүх `expo-*` багцыг тухайн SDK-ийн хувилбар руу
> хамт өөрчилнө (`npx expo install --fix` тусална), дараа нь `npx expo-doctor`.

Хоёр төхөөрөмж ижил өрөөний кодтой байх ёстой. Утас, компьютер өөр сүлжээнд
байсан ч ажиллана — дохио Supabase-ээр дамжина.

## Тохируулж болох утгууд

`app/src/lib/live-location.ts` доторх `CFG`:

| Утга | Анхдагч | Тайлбар |
| --- | --- | --- |
| `MIN_INTERVAL` | 3000 мс | Хоёр илгээлтийн хамгийн богино зай |
| `MIN_DIST` | 15 м | Үүнээс бага хөдөлгөөнийг GPS чичиргээ гэж алгасна |
| `HEARTBEAT` | 15000 мс | Хөдлөөгүй ч "амьд байна" гэж мэдэгдэх давтамж |
| `STALE` | 20000 мс | Хүлээн авагч "дохио тасарсан" гэж үзэх хугацаа |

3 сек / 15 м нь батарей ↔ жигд байдлын тохиролцоо; такси төрлийн аппуудын
түгээмэл утга.

## POC-оос гарахын өмнө шийдэх зүйлс

1. **Background tracking.** Одоо зөвхөн апп нээлттэй үед ажиллана. Утас
   түгжигдэх эсвэл апп ард гарахад `watchPositionAsync` зогсоно. Шийдэл:
   `expo-location`-ий background location (`startLocationUpdatesAsync` +
   `expo-task-manager`) — iOS/Android дээр нэмэлт зөвшөөрөл шаардана.
2. **Батарей.** `Accuracy.High` нь GPS чипийг тасралтгүй асаана. Захиалга
   идэвхтэй үед л асааж, дуусмагц заавал `subscription.remove()`.
3. **Нарийвчлал.** УБ-д задгай талбайд 5–15 м, өндөр байшингийн завсар болон
   гэр хорооллын нарийн гудамжинд 30–100 м хүртэл муудна. `accuracy`-г UI
   дээр үргэлж харуулна — одоо тойрог + "±N м" гэж харагдаж байгаа.
4. **Эрх.** Одоо `/track` нь **нээлттэй** зам (`src/proxy.ts` дотор
   тэмдэглэсэн). Production дээр захиалгын токеноор хамгаалж, зөвхөн тухайн
   захиалгын үйлчлүүлэгч харах ёстой.
5. **Хувийн мэдээлэл.** Байршлыг зөвхөн захиалга идэвхтэй үед дамжуулж,
   дуусмагц зогсооно. Broadcast нь хадгалдаггүй тул түүх үлдэхгүй — гэхдээ
   хожим DB-д бичих бол хадгалах хугацааг тодорхойл.
6. **Газрын зураг.** OpenStreetMap tile нь үнэгүй ч арилжааны ачаалалд
   тохиромжгүй. Mapbox эсвэл Google Maps руу шилжихээр төлөвлө.
